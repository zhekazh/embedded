# Залізо на руках — інвентар і релевантність

> Довідник: що фізично є на столі, який навик кожна штука дає і в який модуль
> роадмапа лягає. Мета — не перепитувати на кожному модулі й не витрачати час
> на філер. Звірено з `ROADMAP_EMBEDDED.md`.

## Плати та інструменти

| Пристрій | Що це | Роль |
|---|---|---|
| **ESP32 Starter Kit** | плата ESP32-**WROOM-32** (Xtensa LX6, dual-core) + сенсори в кейсі | основна плата практики, M5+ |
| **Логічний аналізатор** | FX2 (Cypress CY7C68013A), 8 каналів, USB | дебаг протоколів, M5/M6 |
| **Arduino Uno** | ATmega328P (AVR 8-bit), 5 В логіка | AVR-пісочниця для M3 (не портфоліо) |
| **KY-набір** | 37 модулів-сенсорів/виходів | M1 фізика + кілька корисних драйверів |

---

## ESP32 Starter Kit — вміст

- Плата **ESP32-WROOM-32**
- **OLED 0.96"** (SSD1306, I2C 0x3C) — ідеальна перша ціль для I2C-драйвера з нуля (M5)
- Макетна плата 830 точок
- **DHT11** (temp/humidity, однопровідний таймінговий протокол)
- **PIR HC-SR501** (рух)
- IR-модуль перешкоди
- Фоторезисторний модуль
- Реле 2-канальне 5 В
- Потенціометр 10 кОм
- Активний + пасивний баззер
- Кнопки SWT-9 з ковпачками ×6
- LED (червоні/жовті/зелені ×5 + 1 RGB)
- Резистори 220 / 1к / 10к (×10 кожних)
- Провід Dupont (M-M, M-F, F-F, 200 мм)
- USB-MicroUSB кабель + кейс

> Прошивка/монітор — через USB-UART міст (CP2102/CH340). **Вбудованого USB-JTAG немає**
> (це не S3/C3) — деталі в «Граблі».

---

## KY-набір — рейтинг релевантності

### Варте реальної практики

| Модуль | Що це | Навик | Куди в роадмап |
|---|---|---|---|
| **KY-040** | енкодер (квадратурний) | декодування на перериваннях + debounce; реальний патерн (гімбали, моторний фідбек) | M3 (ISR + state machine) |
| **KY-022** | IR-приймач (VS1838B, протокол NEC) | **найкраща перша ціль для логічного аналізатора** — захопити кадр у PulseView, розкласти за таймінгами | M5 (+ доповнює M2 framing) |
| **KY-024 / KY-035 / KY-003** | датчики Холла (аналог + цифра) | Холл → комутація BLDC, RPM, позиція; аналоговий вихід = ADC, цифровий = компаратор/переривання | M1 (ADC) / M5 |
| **KY-001** | цифровий temp (ймовірно DS18B20) | окрема шина **1-Wire** із суворими таймінгами | M5 (драйвер) |
| **KY-015** | DHT11 (temp/humidity) | однопровідний таймінговий драйвер без бібліотеки | M5 (драйвер) |
| **KY-023** | двоосьовий джойстик | 2 аналогові осі + кнопка → ADC + мок органу керування | M8 |
| **KY-037 / 038 / 039 / 031 / 002** | мікрофон / звук / пульс / удар / вібрація | зашумлені аналогові сигнали → матеріал під цифрові фільтри | M8 (FIR/IIR, ковзне середнє) |

### M1-філер (GPIO in/out, далі цінності нема)

KY-004 (кнопка), KY-005 (IR-LED), KY-006/012 (зумери), KY-008 (лазер),
KY-009/011/016/029/034 (LED), KY-010 (оптичний переривник), KY-013/028 (аналог. temp),
KY-017/020 (нахил), KY-018 (фоторезистор), KY-019 (реле 1-кан), KY-021/025 (геркон),
KY-026 (вогонь), KY-027 («чарівне світло»), KY-032 (перешкода), KY-033 (лінія),
KY-036 (дотик).

> Корисні для M1 (debounce, pull-up/down, плаваючий вхід, PWM, полярність), далі — ні.

---

## Логічний аналізатор (FX2) — нюанси

- **8 каналів, USB-живлення, максимум 24 MSa/s** (24/16/12/8/4/2/1 МГц і нижче), буфер ~10 000 семплів/канал.
- Софт — **sigrok / PulseView**, **не** офіційний Saleae Logic 2 (детектить клон і блокує).
- I2C (100/400 кГц), UART, низькошвидкісний SPI — з головою. **SPI > ~6 МГц** — запас по Найквісту вже хиткий (треба 4–10× частоти сигналу).
- Під WSL — passthrough через `usbipd-win`.

---

## Arduino Uno — навіщо, якщо не портфоліо

Uno **не портфельний актив** (AVR 8-bit, не цільова Cortex-M/Xtensa). Але корисний як
**м'який вхід у register-level мислення для M3**: даташит ATmega328P короткий і читабельний
(`DDRB`, `PORTB`, таймери в кілька регістрів) проти 700-сторінкового ESP32 TRM. Голий
`avr-gcc` без Arduino-обгорток → чистий «reset → main(), копаю регістри» перед Xtensa.
Сходинка драбини, не пет-проєкт.

---

## Чого бракує (докупити під M8 / Project 1)

Жоден набір не закриває портфельних сенсорів для телеметрійного вузла:

- **MPU-6050** — IMU (акселерометр + гіроскоп)
- **NEO-6M** — GPS
- **BME280** — барометр / температура

---

## Граблі / застереження

- **WROOM-32 без вбудованого USB-JTAG** → причина: на відміну від S3/C3, класичний
  WROOM прошивається через USB-UART, а JTAG — це окремі піни GPIO12-15 + зовнішній
  адаптер; фікс: або докупити **ESP-Prog** (~$10–15) для JTAG на залізі, або тримати
  JTAG-дебаг на **Wokwi** (емулятор), а на реальній платі — `esp_log` / app trace.
- **5 В ↔ 3.3 В** → причина: Uno — 5 В логіка, ESP32 — 3.3 В і **не толерантний до 5 В**;
  KY-модуль при живленні 5 В дає 5 В на цифровому виході, що може спалити GPIO ESP32;
  фікс: живити KY від 3.3 В де можливо, або level shifter / дільник на цифрових виходах.
